import { Card } from "@/components/shell/Card.tsx";

/**
 * SwapPanel — stub placeholder. Real implementation lands in Phase 3
 * commits below this merge commit (P3-001 through P3-007).
 */
export function SwapPanel() {
  return (
    <Card className="flex-1 flex flex-col items-center justify-center text-center p-8 gap-4">
      <p className="text-sm text-text-dim">Swap UI lands in Phase 3 (P3-001 through P3-007).</p>
    </Card>
  );
}
